---
import BlogList from './BlogList.astro';
import { Blog, sortByDate } from '../util/blog';

const blogs: Blog[] = await Astro.glob('../pages/blogs/*.md');
const { count } = Astro.props;

const latestBlogs = sortByDate(blogs).splice(0, count); 
---

<BlogList blogs={latestBlogs}></BlogList>

